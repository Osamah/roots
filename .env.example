# Postgres connection (matches docker-compose.yml)
DATABASE_URL="postgresql://roots:roots@localhost:5432/roots?schema=public"

# NextAuth / Auth.js — generate a real secret with: openssl rand -base64 32
AUTH_SECRET="change-me"
# Derive the base URL from the incoming request (works on any dev port).
AUTH_TRUST_HOST="true"
